{
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": ""
  },
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "us-west-2",
    "source_ami": "ami-dc4fbebc",
    "instance_type": "t2.large",
    "ssh_username": "ubuntu",
    "ami_name": "base-30-collections {{timestamp}}"
  }],
  "provisioners": [
    {
      "type": "ansible",
      "playbook_file": "./30-collections.play"
    }
  ]
}
